<h1>Offers</h1>

<ul>
  <% @offers.each do |offer| %>
    <li>
      <%= link_to offer.name, offer_path(offer) %>
      &mdash; <%= number_to_currency(offer.price_cents / 100.0, unit: "€") %>
    </li>
  <% end %>
</ul>
